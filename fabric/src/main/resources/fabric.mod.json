{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": [
    "${mod_author}"
  ],
  "contact": {
    "homepage": "${homepage}",
    "sources": "${sources}"
  },
  "license": "${mod_license}",
  "icon": "assets/${mod_id}/logo.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      {
        "adapter": "kotlin",
        "value": "io.github.techtastic.kubevs.fabric.KubeVSFabric"
      }
    ],
    "client": [
      {
        "adapter": "kotlin",
        "value": "io.github.techtastic.kubevs.fabric.KubeVSFabric${'$'}Client"
      }
    ],
    "preLaunch": [
      {
        "adapter": "kotlin",
        "value": "io.github.techtastic.vs_addon_template.fabric.ValkyrienPreLaunch"
      }
    ]
  },
  "mixins": [
    "kubevs-common.mixins.json",
    "kubevs.mixins.json"
  ],
  "depends": {
    "minecraft": "${minecraft_version}",
    "valkyrienskies": ">=${vs2_version}",
    "fabricloader": ">=${fabric_loader_version}",
    "fabric-api": "*",
    "architectury": ">=${architectury_version}",
    "kubejs": "*"
  }
}